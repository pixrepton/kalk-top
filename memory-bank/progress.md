# Progress

## Ekosystem TOP-INSTAL (wpisy poza samym kalk-top)

Chronologia cross-repo **2026-05-15…23:** `knowledge/timeline/2026-05.md`. Szczegóły gmail-agent: `../gmail-agent/memory-bank/agent-handover.md`. Cieplo worker: `../topinstal-cieplo-orchestrator/memory-bank/agent-handover.md`.

| Data          | Powiązanie z kalk-top                                                                                |
| ------------- | ---------------------------------------------------------------------------------------------------- |
| 2026-05-20–21 | Cieplo worker VPS — wspólny Postgres/env z pipeline oferty; brak pełnego `poll` do `calculate-offer` |
| 2026-05-22    | P0 registry — korelacja spraw mailowych z workflow Cieplo (`case_id` ↔ `workflow_id`)                |
| 2026-05-23    | Daszek feed — sprawy z mailbox memory; kalk-top nadal SoT OfferDTO                                   |

---

- 2026-06-04 (session closeout): domknięto backlog OZC/PDF 1–8 z właścicielem. **Kod:** naprawa poddasza (`resolveAtticHeatingContext` — tylko jawne Poddasze); roczna energia `utilizationFactor=0.72`; runtime :8091, `RestJsonGuard`, Playwright proof/E2E pricing; regresja poddasza w `ozc-full-audit.regression.php`. **Dokumentacja:** `BACKLOG_RESOLUTIONS_2026-06-04.md`, zaktualizowano `ozc-professional-method-audit.md`, `offer-dto-pdf-mapping-audit.md` (OPEN WORK §7), `memory-bank/*`, `SOURCE_OF_TRUTH_INDEX.md`. **Problem 7:** audyt mapowania OfferDTO→PDF — implementacja w `top-instal-generator` **otwarta**. **Usunięto z backlogu:** luki pomp, sync Panasonic jsonl, bypass `ozcResult`. Commit `152cca1`. Weryfikacja: `npm run test:contract` PASS.
- 2026-06-04: synchronizacja dokumentacji z kodem po audycie ekosystemowym. `ozc-professional-method-audit.md` — tabela Code sync status; usunięto martwe linki do `test:engine-parity`, `migration-plan.md`, `IMPLEMENTATION_BACKLOG_FROM_DOCS.md`; zaktualizowano `memory-bank/current-state.md`, `HANDBOOK.md`, `repo-rules.md`, `KALK_TOP_AGENT_HARNESS.md`, `TOPINSTAL_ECOSYSTEM_STATE.md`, `COMPAT_POINTERS.md`, `OZC_INPUT_REQUIREMENTS.md`. Weryfikacja: pliki istnieją; `catalog_items.jsonl` 237 rekordów; golden Panasonic 41/41 PASS.
- 2026-05-20: audyt detektyw silników obliczeniowych (`docs/architecture/engine-calculation-detective-report-2026-05-20.md`): P0 w OZC (addytywne kW, HDD bez eta_rec, geometria poddasza, floor_area vs heated_area, koszty SCOP/CWU); CWU/buffer/pricing bez P0 w izolacji; harnessy PHP PASS oprócz `engine-parity.php` (brak `clonePumpTableFallback` w configurator JS). Zaktualizowano wszystkie `*.graph.md` + `engine-graphs.json` po `--force` (8615/15106).
- 2026-05-20: powtórzono eksport grafów silników z GitNexus MCP (repo `kalk-top` zindeksowane: 8604 nodes, 15096 edges). Zaktualizowano `docs/architecture/engine-graphs/engine-graphs.json` (blok `gitnexus`, UIDs klas/metod, CALLS z `context`/`cypher`, orchestracja `CalculateOfferUseCase.execute`) oraz README grafów. Potwierdzono łańcuch ryzyka OZC: `calculateOZC` → `computeAdditiveCorrectionsKw` (L680) + parity JS `kalkulator/engine/ozc/ozc-engine.js`. Weryfikacja: MCP `list_repos` pokazuje `kalk-top`; `context`/`cypher` na silnikach OK; `query` nadal ostrzega o braku FTS do pełnego `--force` rebuild.
- 2026-05-20: wyrównano agent harness do poziomu gmail-agent (Fazy A+B+C). Dodano: `LOCAL_WORKSPACE_RULES.md`, `.agents/SKILL_ROUTER.md`, `00-kalk-top-core-router.mdc` (jedyna reguła always-on), MCP `kalk-top-repo-assistant` + Playwright w `.cursor/mcp.json`, `.cursorignore`, skille współdzielone (karpathy, cursor-codex-harness, context-budget, gitnexus, kalk-top-offerdto-contract), `docs/dev/KALK_TOP_AGENT_HARNESS.md`, `KALK_TOP_GITNEXUS.md`, `memory-bank/navigation.md`, hook `after-file-edit-lint`, slash commands `verify-*`, `scripts/agent-harness-preflight.mjs`, `.codex/` (reviewer, php-pro, typescript-pro). Weryfikacja: `node scripts/agent-harness-preflight.mjs` OK, `npm run test:contract` PASS, `npm run verify:js` PASS.
- 2026-05-06: utworzono osobne grafy wiedzy/zależności/obliczeń dla silników `ozc`, `selection`, `cwu`, `buffer`, `pricing` pod `docs/architecture/engine-graphs/` oraz maszynowy `engine-graphs.json` gotowy do późniejszego importu do GitNexus. Ponieważ w tej sesji MCP GitNexus nie był dostępny jako wywoływalne narzędzie, grafy zapisano w repo jako Markdown + JSON. Dodano też `docs/architecture/ozc-professional-method-audit.md`, porównujący obecne OZC z praktyką PN-EN 12831-1 / ISO 6946 / ISO 52016 i polską metodyką. Najważniejsze ustalenie: `computeAdditiveCorrectionsKw()` prawdopodobnie podwójnie liczy/kompensuje okna, drzwi, rekuperację i piwnicę, bo te efekty są już w fizycznym `U*A*dT` oraz wentylacji. Dodatkowo annual/HDD używa pełnego `HV` bez `eta_rec`, co może zawyżać roczną energię/koszty przy rekuperacji. Weryfikacja: poprawny parse JSON (`engine-graphs.json OK`) i próbka JS parity na wariantach OZC pokazująca drift addytywny od `-1.20` do `+1.57` kW.
- 2026-04-14: pełny audyt dokumentacji (114 plików .md). Zaktualizowano: `docs/discovery/repo-discovery.md` (nowe sekcje: Panasonic pipeline, offer-PDF bootstrap); `docs/DOC_INVENTORY_AND_CLASSIFICATION.md` (sekcja 2b Panasonic); `docs/DOC_CONFLICTS_AND_GAPS.md` (sekcja 9); `docs/IMPLEMENTATION_BACKLOG_FROM_DOCS.md` (zrealizowane pozycje); `docs/SOURCE_OF_TRUTH_INDEX.md` (sekcja 5b Panasonic); `docs/README.md` (tabela Panasonic); `docs/HANDBOOK.md` (offer PDF generator, Panasonic catalog); `docs/ecosystem/TOPINSTAL_ECOSYSTEM_STATE.md` (changelog 2026-04-14); `docs/runbooks/offer-generator-integration-audit.md` (sekcja Bootstrap hardening); `docs/panasonic_catalog_update_workflow.md` (wskazanie v2 jako produkcyjnego); `knowledge/panasonic/accessories_and_controls.md` (poprawka klasyfikacji); `knowledge/panasonic/pricing_usage_notes.md` (poprawka encoding); `memory-bank/decisions.md` (dwa nowe wpisy).
- 2026-04-14: wykonano pass 2 hardeningowy dla danych Panasonic. `panasonic_catalog_extract_v2.py` zastąpił pass-1. Wynik: 237 rekordów, 10 kategorii, 0 brakujących modeli, 235/237 z ceną, golden test 41/41 PASS. Root cause regresji pass-1: brak profilu per sekcja + ceny wyłącznie z parsowania tekstu zamiast spatial word matching. Pass 2 naprawił oba problemy: profil ekstrakcji per strona + coordinate-based price matching z min_price per kategoria.
- 2026-04-14: wykonano pełny pipeline danych Panasonic dla `panasonic/Panasonic_cennik_pompy_ciepla_03.2026.pdf` w podejściu JSON-first. Dodano skrypty `scripts/panasonic_catalog_extract.py`, `scripts/panasonic_catalog_normalize.py`, `scripts/panasonic_catalog_validate.py`, `scripts/panasonic_catalog_compare.py`; schematy `schemas/panasonic_catalog_item.schema.json`, `panasonic_catalog_family.schema.json`, `panasonic_pricing_fact.schema.json`, `panasonic_context_packet.schema.json`; skill-pack operacyjny `panasonic/agent-skills/*`; i workflow aktualizacji `docs/panasonic_catalog_update_workflow.md`. Wygenerowano wymagane warstwy raw/normalized/memory/knowledge/context oraz raporty `reports/panasonic_*` (audit, qc, schema validation, quality summary, baseline compare). Wynik obecnego przebiegu: 236 rekordów, 0 błędów schematu, 0 braków `page/source_doc`, 39 rekordów `heat_pumps` bez modelu (oznaczone do manual review).

- 2026-04-14: naprawiono regresję bootstrapu generatora PDF oferty. `wp-adapter/mail-ingress/WorkflowConfig.php` i `OfferDocumentsGeneratorClient.php` przestały opierać się wyłącznie na jednej ścieżce `kalk-top/gmail-agent/...` (po splitcie workspace była błędna). Dodano wielościeżkowy include (`kalk-top/gmail-agent` + sibling `../gmail-agent`) oraz fallback definicji klas in-repo, dzięki czemu `TopInstal_OfferDocumentsGeneratorClient` jest dostępny nawet gdy zewnętrzne drzewo nie jest obecne. `heatpump-calculator.php` dostał diagnostyczny log `offer documents generator client unavailable` z informacją o klasach i bootstrapie. Dodano też probe regresyjny `scripts/probes/offer-doc-client-probe.php`. Weryfikacja: `php -l wp-adapter/mail-ingress/WorkflowConfig.php`; `php -l wp-adapter/mail-ingress/OfferDocumentsGeneratorClient.php`; `php -l heatpump-calculator.php`; `php scripts/probes/offer-doc-client-probe.php`.

- 2026-04-14: zrewidowano zaleznosc miedzy CWU i rekomendacja all-in-one po sprawdzeniu PDF-ow Panasonica dla serii K. `SelectionEngine` nie traktuje juz `250/300/350 l` jako ogolnego pozwolenia na AIO 260 l; taki wariant pojawia sie tylko wtedy, gdy wybrany split ma jawna pare `aio_model_large_cwu` w katalogu Panasonica. Dla slabszych rodzin split bez takiej pary AIO znika calkowicie i zostaje tylko split + zasobnik wolnostojacy; dla 400 l+ AIO nadal znika zawsze. Frontendowy fallback w `konfigurator/configurator-unified.js` juz mial te same progi, a dodatkowo rozszerzono sanitizator mojibake o CP1252/UTF-8 przypadki typu `powrĂłt` i `JakoĹ›Ä‡`. Weryfikacja: `php core/application/harness/selection-aio-cwu.regression.php`, `npm run test:contract`, `node kalkulator/js/ui.regression.test.js`.

- 2026-04-14: naprawiono krytyczny bootstrap WordPress pluginu przy aktywacji. `wp-adapter/mail-ingress/WorkflowConfig.php` i `wp-adapter/mail-ingress/OfferDocumentsGeneratorClient.php` byĹ‚y wrapperami, ale robiĹ‚y bezwarunkowe `require_once` do nieistniejÄ…cego w tym workspace `gmail-agent/wp-adapter/mail-ingress/*`, co mogĹ‚o koĹ„czyÄ‡ siÄ™ fatalem juĹĽ przy sandboxowym include pluginu podczas aktywacji. Wrappery teraz Ĺ‚adujÄ… kanoniczne pliki tylko gdy faktycznie istniejÄ…; brak tej opcjonalnej warstwy nie powinien juĹĽ blokowaÄ‡ uruchomienia kalkulatora. Weryfikacja: `php -l wp-adapter/mail-ingress/WorkflowConfig.php`, `php -l wp-adapter/mail-ingress/OfferDocumentsGeneratorClient.php`, `php -r "define('ABSPATH', __DIR__ . '/'); function add_action() {} include 'c:/Users/compg/Desktop/kalk-top/heatpump-calculator.php'; echo 'bootstrap-ok';"`.

## Completed

- 2026-04-14: domknięto semantyczny pass dla PDF „profil energetyczny”. `offerProjection/core.js` i fallback w `offerPayload.js` przestały mieszać `floor_area` z `heated_area`, więc footprint/„powierzchnia zabudowy” nie jest już dorabiany z metrażu ogrzewanego. `downloadPDF.js` wycina z energy snapshotu wskaźniki o słabej wartości biznesowej (`Pow. całkowita`, `Moc CWU`, `Temp. średnioroczna`, `Moc średnia`, `Zużycie dzienne / 365`), zachowuje źródłową kolejność porównania kosztów, usuwa ryzyko wycieku surowego kodu pompy na rzecz klientowej nazwy modelu i buduje bardziej uzasadniony opis doboru. `pdfGenerator.js` dostał bezpieczniejsze etykiety budynku/izolacji, brak placeholdera „typ: standardowy”, pozytywny komunikat przy pustych punktach biwalentnych oraz noty interpretacyjne dla metrażu, kosztów i strat ciepła. Dołożono regresje dla footprint-vs-heated-area oraz dla końcowego HTML energy PDF. Weryfikacja: `npm run verify:js`, `node --check kalkulator/js/pdfGenerator.js`, `node kalkulator/js/offerProjection.regression.test.js`, `node kalkulator/js/p0.regression.test.js`.
- 2026-04-14: business-polish dla sekcji „Rekomendowana moc pompy ciepła” został dopięty do tej samej rekomendacji, którą użytkownik widzi w konfiguratorze. `downloadPDF.js` preferuje teraz `selection.recommendedModels[0]` jako źródło mocy/fazy/nazwy modelu, zamiast budować sekcję wyłącznie z własnych fallbacków po `capacity_kW`; copy sekcji zostało też zmiękczone do „dobór wstępny” z potwierdzeniem po weryfikacji technicznej, żeby nie sugerować fałszywej pewności. Dla referencji wizualnej wyrenderowano i obejrzano PDF `C:\Users\compg\Downloads\Raport_Cieplny_Budynku_2026-04-13.pdf` jako PNG; układ i hierarchia energy PDF pozostały w tym samym stylu (górna belka, sekcje z czerwonym podkreśleniem, oszczędna typografia), ale bez kopiowania ryzykownego badge’a „najniższy koszt”. Dołożono regresję wymuszającą zgodność sekcji pompy z rekomendowanym snapshotem konfiguratora. Weryfikacja: `npm run verify:js`, `node kalkulator/js/p0.regression.test.js`, `node kalkulator/js/offerProjection.regression.test.js`.
- 2026-04-14: uproszczono finalne copy energy PDF zgodnie z decyzją produktową. Usunięto z raportu dodatkowe zdania pod sekcją doboru pompy, notę o powierzchni ogrzewanej vs zabudowie oraz notę metodologiczną pod kosztami; zniknęła też `Powierzchnia zabudowy`. Sekcja pompy pokazuje już tylko moc i fazowość z tego samego rekomendowanego snapshotu co konfigurator, bez dodatkowej linii z modelem. Etykiety izolacji wróciły do prostego `Izolacja ścian / dachu / podłogi`. Weryfikacja: `npm run verify:js`, `node kalkulator/js/p0.regression.test.js`.
- 2026-04-14: odcięto PDF „profil energetyczny” od treści handlowych i debugowych: `buildEnergyReportData` składa bezpieczny snapshot (OZC, budynek, dobór pompy, biwalenty, koszty z SCOP, straty), `createPdfPagesMarkup` rozgałęzia `energy` na `buildEnergyReportPdfMarkup`, wspólna `buildPdfBuildingFieldRows` dla parametrów budynku; ścieżka oferty przez generator zewnętrzny bez zmian. Weryfikacja: `node --check` na zmienionych plikach, `node kalkulator/js/offerProjection.regression.test.js`, `npm run verify:js` + `verify:js:regressions` (pełne `npm run verify` urwane na brakującym `gmail-agent` w tym środowisku).
- 2026-04-05: finished a standalone cleanup pass for the extracted `gmail-agent/` workspace. Rewrote moved AGENTS/docs/memory-bank content so it no longer depends on parent `kalk-top` paths, added a root `.gitignore` plus subsystem-local `AGENTS.md` files under `gmail-agent/`, removed dead external markdown links from moved runtime docs, and re-verified the standalone-facing Python CLI help, PHP bridge lint, Node syntax, and markdown-link integrity.
- 2026-04-05: split the Gmail/Groq intake runtime, Daszek, selected mail-ingress bridge code, runtime docs, and Gmail verification artifacts into the new top-level workspace `gmail-agent/`. Added a dedicated `gmail-agent/AGENTS.md`, `.cursor/rules/*`, `.agents/skills/*`, and `memory-bank/*`, left thin compatibility wrappers in the old outer-root paths, and updated key kalk-top docs so Gmail-agent ownership is now explicit without breaking the existing kalk-top cockpit.
- 2026-04-05: completed a Gmail Intake consolidation pass focused on runtime predictability, artifact clarity, context quality, and architecture docs without changing the Gmail-first design. `tools/gmail_audit/gmail_intake.py` now supports explicit `--timebox-seconds`, `--max-failures`, and `--max-consecutive-failures` stop controls; records `runtime_controls`, `stop_reason`, and `stop_details`; and preserves partial-completion state more clearly in manifest/checkpoint/summary artifacts. `intake_schema.py` and `artifact_contracts.py` now distinguish `raw_valid`, `normalized_valid`, `repaired_valid`, and `guardrailed_review`, while `eval_shadow.py` and review CSVs surface those validation origins plus projected `case_key` / `case_key_source`. `intake_payload.py` now renders prompt vocabulary from code-owned policy constants and filters noisy reference tokens more conservatively so false case-link candidates like `reference:Ciebie` or `reference:Witaj` do not inflate context quality. `dash_preview.py` now records case-key provenance in downstream intake metadata, `tools/gmail_audit/README.md` was aligned to the strengthened runtime, and a new root doc `README_GMAIL_INTAKE_SYSTEM.md` now describes the real Gmail-first system architecture professionally. Verification completed with `python -m compileall tools/gmail_audit`, `python tools/gmail_audit/gmail_intake.py --help`, `python tools/gmail_audit/gmail_intake.py message --help`, targeted inline Python checks for prompt rendering, reference extraction, review-row enrichment, and Daszek preview case-key projection.
- 2026-04-04: hardened the Groq inference runtime for `tools/gmail_audit/` without changing the overall V1 architecture. `intake_payload.py` now keeps the full frozen source snapshot for audit but builds separate compact inference payloads with explicit section budgets, quote/history trimming, footer/noise cleanup, and ordered inference modes (`compact`, `reduced_compact`, `minimal_safe`); `groq_client.py` now records retry metadata, adds backoff+jitter plus runtime cooldown for 429 pressure, and degrades to smaller inference modes on `413` / sustained throttling; `intake_schema.py` now applies safe snapshot-backed normalization and a conservative local repair pass that can downgrade invalid outputs to `review`; and `gmail_intake.py` now stores raw model output, normalized candidates, repair attempts, and final validation records separately. Static verification passed with `py_compile`, `doctor --gmail-source google_api` stayed clean, and a live `shadow-run --days 1 --limit 3 --context-limit 2 --keep-going --gmail-source google_api` produced two consecutive valid `compact` outputs with zero schema-invalid or 413 failures before the third item hit lingering Groq-side throttling.
- 2026-04-04: switched Gmail Intake into a practical operational fetch mode that no longer depends on the Groq beta Gmail connector. Added `tools/gmail_audit/google_gmail_api.py` for direct Gmail API profile/search/read/thread access over the existing refresh-token flow; rewired `gmail_fetch.py` into a source selector with `google_api` as the default and `groq_connector` retained as optional/experimental fallback; added `--gmail-source` to `gmail_intake.py`; extended context fetch to use direct thread reads when available; and kept frozen snapshots, shadow mode, eval, validation, and Daszek preview artifacts unchanged above the fetch layer. Live verification succeeded with `python tools/gmail_audit/gmail_intake.py doctor --verbose --gmail-source google_api` and `python tools/gmail_audit/gmail_intake.py period --days 1 --limit 1 --context-limit 1 --gmail-source google_api`, while `doctor --gmail-source groq_connector` still reproduces the known connector-side 401 for comparison.
- 2026-04-04: tightened Gmail auth runtime diagnostics in `tools/gmail_audit/` around the current 401 investigation. `config.py` now records config precedence per Google key (`process_env` vs `.env.local`), warns when process env overrides a file value, and normalizes an accidental `Bearer ` prefix on `GOOGLE_ACCESS_TOKEN`; `google_oauth.py` now exposes `active_auth_mode`, `token_source`, expected `gmail.readonly`, and a direct-Google runtime check (`gmail/v1/users/me/profile` plus `tokeninfo`); `groq_client.py` now forces refresh-token resolution before every Gmail connector call, logs a debug-safe auth summary, and guarantees the connector receives the raw access token string rather than a `Bearer`-prefixed variant; and `gmail_intake.py doctor` / preflight now report direct Google success separately from connector failure. Live verification with the current credentials confirms: values come from `tools/gmail_audit/.env.local`, refresh flow wins over static `GOOGLE_ACCESS_TOKEN`, direct Google auth succeeds, and the remaining 401 is specifically the Groq Gmail connector still claiming missing credentials.
- 2026-04-04: compared the Gmail Intake Google/Groq auth path against official Google Developer OAuth/Gmail scope docs and official Groq Responses API / Remote MCP connector docs, then reconciled the implementation accordingly. `config.py` now treats `GOOGLE_CLIENT_SECRET` as optional where Google docs allow it, enforces HTTPS on `GOOGLE_TOKEN_ENDPOINT`, and keeps refresh-token mode minimal; `google_oauth.py` now sends `client_secret` only when configured, surfaces `error_subtype` like `invalid_rapt`, and keeps Google-token failure messages closer to official semantics; README and local specs now explicitly note restricted Gmail scope, short-lived access tokens, Testing-mode refresh-token risk, and the fact that this runner still remains a local-only operator tool rather than a general auth service.
- 2026-04-04: after temporarily restoring `tools/gmail_audit/.env.example` to its pre-sanitization local content at the user's explicit request, moved the real values into `tools/gmail_audit/.env.local` and returned `.env.example` to a safe placeholder template. The intended steady state is again local secrets in `.env.local` and non-secret examples in `.env.example`.
- 2026-04-04: performed the first real runtime verification with the restored local credentials. `python tools/gmail_audit/gmail_intake.py doctor` now loads `.env.local`, recognizes `refresh_token_auto`, and fails at Gmail connector preflight rather than config; a direct call through `google_oauth.py` successfully refreshes a bearer token; direct Google checks against `https://gmail.googleapis.com/gmail/v1/users/me/profile` and `https://www.googleapis.com/oauth2/v3/tokeninfo` succeed and confirm `gmail.readonly`; but the Groq `connector_gmail` `get_profile` call still returns a tool-level 401 saying authentication credentials are missing. A first live `period --days 1 --limit 1` run therefore stops cleanly as `failed_auth` in preflight, and ad-hoc connector-shape experiments with raw `authorization`, `Bearer`-prefixed `authorization`, and `headers.Authorization` all fail the same way.
- 2026-04-03: added an isolated local helper at `tools/gmail_audit/` for read-only Gmail mailbox auditing through Groq Responses API + official `connector_gmail`, including CLI commands (`general-audit`, `rules-proposal`, `important-mails`), prompt files, `.env.example`, README, robust response parsing, and local smoke checks (`py_compile`, `--help`, missing-ENV error path). No kalk-top calculation, DTO, REST, or downstream automation surfaces were touched.
- 2026-04-03: ran live verification of `tools/gmail_audit/` against Groq. Confirmed MCP connector wiring, captured raw Groq response for diagnostics, fixed Windows stdout UTF-8 handling, and removed `json_schema` mode from connector-backed requests because Groq rejects JSON mode combined with tool calling. Current blocker is the provided Google token being rejected by Gmail as `401 Invalid Credentials`.
- 2026-04-03: added `tools/gmail_audit/full_mailbox_categorize.py` to support whole-mailbox incoming classification with checkpointed paging via `search_emails` and deterministic intake categories. Live run discovered 7706 incoming messages (`to:me -in:spam -in:trash`) and produced a partial classification for the first 2145 before the Gmail access token expired mid-run; partial outputs were saved under `tools/gmail_audit/reports/incoming_mailbox_classification_partial.*`.
- 2026-04-03: produced the first discovery/spec package for `tools/gmail_audit` under `tools/gmail_audit/reports/`: `MAILBOX_DISCOVERY_REPORT.md`, `CLASSIFICATION_AUDIT.md`, `BUSINESS_SIGNALS_CATALOG.md`, `CASE_STATE_AND_ACTION_MODEL.md`, `INTAKE_AGENT_V1_SPEC.md`, and `INTAKE_OUTPUT_SCHEMA_V1.json`. The package critiques the current mailbox taxonomy, derives business signals from the observed inbox, and proposes the first serious intake-decision model for future Daszek/case integration.
- 2026-04-03: converted the Gmail-intake discovery/spec package into an implementation-ready V1 design package under `tools/gmail_audit/reports/`: `IMPLEMENTATION_PLAN_V1.md`, `AGENT_PROMPT_V1.md`, `CONFIDENCE_AND_REVIEW_POLICY.md`, `SHADOW_MODE_SPEC.md`, `LOCAL_RUNNER_SPEC.md`, `DASZEK_MAPPING_V1.md`, and `TESTING_AND_EVAL_PLAN.md`. The new package defines the safe first rollout as a local shadow-mode runner using Groq plus the official Gmail connector, strict JSON validation, human review loops, and preview-only mapping to future Daszek objects.
- 2026-04-03: implemented the first Gmail Intake Intelligence MVP runner under `tools/gmail_audit/`: `gmail_intake.py`, `gmail_fetch.py`, `intake_payload.py`, `intake_schema.py`, `dash_preview.py`, and `eval_shadow.py`, plus intake prompts and schema files. The MVP supports `message`, `batch`, and `period`, writes local run directories, performs schema-guided Groq decisions plus semantic validation, exports a shadow review CSV, and generates preview-only Daszek mapping artifacts. Verification completed: `python -m py_compile` on all new modules and `python tools/gmail_audit/gmail_intake.py --help` / `message --help`.
- 2026-04-03: adapted `Daszek/` for Gmail Intake preview compatibility without enabling live mutation from the intake runner. The local Daszek item model now supports `kind`, `priority`, `note`, `tags`, `external_ref`, and `intake`; the REST API validates richer payloads; cyclic create preserves the expanded contract; the board UI renders intake metadata and review context; `Daszek/SCHEMA.md` and `Daszek/examples/sample-tasks.json` were aligned to the new shape; and `tools/gmail_audit/dash_preview.py` now emits preview requests compatible with the actual local `POST /wp-json/daszek/v1/tasks` endpoint. Verification completed: `php -l` on touched PHP files, `node --check Daszek/public/app.js`, `python -m py_compile tools/gmail_audit/dash_preview.py`, and a Python smoke test confirming `create_case_and_task` preview generation.
- 2026-04-03: completed a hardening pass on Gmail Intake V1 and the local Daszek target. `tools/gmail_audit/gmail_intake.py` now supports `shadow-run`, `rerun`, and `eval` in addition to `message`, `batch`, and `period`; writes manifest/checkpoint/selection/source/model/validation/output/preview/error artifacts; supports frozen reruns without Gmail access; handles mixed live/frozen batches; and now uses unique run ids with a random suffix to avoid concurrent run collisions. Security hygiene was tightened by removing real-looking secrets from `tools/gmail_audit/.env.example`, adding Gmail-audit env/run/debug/cache paths to `.gitignore`, and redacting auth-like values in Groq artifact/error storage. Supporting modules were hardened for retry/backoff, subject/thread heuristics, conservative case-link candidates, stronger semantic validation, preview payload validation, and richer shadow-eval outputs. Additional reliability fix: `Daszek/public/app.js` now propagates HTTP status on API errors so the UI can react to 401 explicitly.
- 2026-04-03: hardened runtime verification for Gmail Intake V1. Completed checks: `python -m py_compile` for intake modules, `node --check Daszek/public/app.js`, `php -l` for touched Daszek PHP files, CLI help for `message`, `batch`, `period`, `shadow-run`, `rerun`, and `eval`, invalid-JSON and schema-invalid validation smoke tests, preview payload generation smoke test, frozen `batch` / `shadow-run` / `rerun` smoke tests with invalid Groq key, `eval` smoke test on a local fixture run, missing-ENV smoke tests, and live `message` / `period` smoke tests that now fail cleanly with recorded Gmail `401 Invalid Credentials` artifacts at `fetch` / `selection` stage.
- 2026-04-03: added the first real Daszek mutation path for Gmail Intake. New module `tools/gmail_audit/daszek_client.py` performs Daszek REST login, keeps a session with CSRF token, loads existing tasks, deduplicates against existing `gmail_intake` items by `kind` + `external_ref.message_id` + `case_key`, and creates real items through the local Daszek API. `gmail_intake.py` now supports `--push-daszek`, writes `daszek_push_results.jsonl`, and reports `daszek_push_requested` / `daszek_push_connected` plus push counters in run summaries. `.env.example` now exposes `DASZEK_BASE_URL`, `DASZEK_LOGIN`, and `DASZEK_PASSWORD`. Verification completed for syntax, CLI exposure of the new flag, duplicate-detection helper behavior, and graceful failure when push is requested without Daszek configuration.
- 2026-04-03: finished the operational closure pass on Gmail Intake V1. Added `doctor` as a real preflight/auth check, switched documented secret handling to `.env.local` or shell env vars, removed `tools/gmail_audit/.env` after finding real leaked credentials there, added a local `tools/gmail_audit/.gitignore`, and re-verified that no Groq or Google tokens remain anywhere under `tools/gmail_audit/`. The runner now records preflight results, error categories, and `failed_auth` / `failed_preflight` statuses; Gmail auth failures fail early with operator-friendly guidance; frozen `shadow-run` and `rerun` complete successfully with a real Groq key; `eval` now writes `eval_summary.md` plus failure-cluster-ready summary fields; contextual guardrails now force review for weak append/update context, self-forward ambiguity, and suspicious new-case choices; Daszek live push now fails early with a clear config preflight when credentials are missing; and docs/specs were reconciled so preview is the default truth while live push is explicitly documented as experimental opt-in runtime behavior.
- 2026-04-03: completed a static-only quality hardening pass for Gmail Intake V1 without running Gmail, Groq, Daszek live push, tests, or smoke commands. Added shared modules `tools/gmail_audit/intake_policy.py`, `artifact_contracts.py`, `artifact_io.py`, and `redaction.py`; moved runtime vocabulary, review/decision policy, Daszek object mapping, artifact filenames, review CSV columns, and write semantics into those modules; refactored `gmail_intake.py`, `intake_schema.py`, `dash_preview.py`, `daszek_client.py`, `eval_shadow.py`, `groq_client.py`, and `intake_payload.py` to use the shared layers; renamed validation artifact guardrail output to `guardrail_flags`; hardened preview contract checks around `object_type` / `kind` / `external_ref`; expanded `.gitignore` for cache/log/temp hygiene; and reconciled README/spec/SCHEMA/prompt docs around `external_ref`, pass-through priority, stable run artifacts, and prompt-schema-runtime alignment.
- 2026-04-03: added practical Google OAuth refresh-token support to `tools/gmail_audit/` without changing the local sidecar architecture. New helper `google_oauth.py` can exchange `GOOGLE_CLIENT_ID` + `GOOGLE_REFRESH_TOKEN` plus optional `GOOGLE_CLIENT_SECRET` for a fresh in-memory access token through the Google token endpoint; `config.py` now accepts either manual `GOOGLE_ACCESS_TOKEN` or refresh-token mode, validates that `GOOGLE_TOKEN_ENDPOINT` stays HTTPS, and no longer over-requires a client secret; `groq_client.py` now resolves Gmail connector auth through that helper and classifies `invalid_rapt` as auth; `gmail_intake.py doctor` and run preflight now expose redaction-safe Google auth mode/preference details; and `.env.example`, `.env.local.example`, README, and local runner docs were updated to match official Google and Groq auth semantics while keeping real secrets out of checked-in examples.
- 2026-04-03: refreshed `backup-tekstowe/` (empty folder) with `scripts/backup-text-copy.ps1`; verified `Copied: 204 files` (docs/markdown/text/pdf snapshot, no application source).
- 2026-04-03: reconciled remaining governance inconsistencies by demoting `docs/ecosystem/schemas/*` to reference-only authority language, moving trust markers to the real top of long-form `docs/agent/*` files, aligning local schema artifacts with runtime `schemaVersion = "1.0"` where repo evidence exists, and recording the accepted future-state directions directly in canonical docs and `memory-bank/decisions.md`.
- 2026-04-02: introduced a documentation-governance layer under `docs/` with new canonical docs for authority routing, governance, agent execution, read priority, entity model, event model, AI OS blueprint, inventory, conflicts/gaps, and implementation backlog.
- 2026-04-02: updated core doc entrypoints and trust markers in `docs/README.md`, `docs/HANDBOOK.md`, `docs/architecture/repo-rules.md`, `docs/contracts/README_NOWE_WEJSCIA_CALCULATE_OFFER.md`, `docs/contracts/dto-and-boundaries.md`, `docs/contracts/API_CALCULATE_OFFER.md`, `docs/ecosystem/README.md`, `docs/ecosystem/TOPINSTAL_ECOSYSTEM_STATE.md`, `docs/ecosystem/TOPINSTAL_ECOSYSTEM_UPDATE_PROTOCOL.md`, and `docs/agent/README.md`.
- 2026-04-02: condensed `memory-bank/active-context.md` and `memory-bank/progress.md` so memory-bank acts as operational context rather than long-form historical narrative.
- 2026-04-02: preserved a future AI input-channel backlog at `docs/agent/AI_INPUT_CHANNELS_FUTURE_OPTIONS.md` and clearly classified it as vision/backlog rather than runtime truth.
- 2026-04-02: added the canonical guide for new `calculate-offer` inputs at `docs/contracts/README_NOWE_WEJSCIA_CALCULATE_OFFER.md`, including the documented runtime-vs-schema drift around `schemaVersion`.
- 2026-03-30: visible pricing was hardened as backend-only via `OfferDTO`; generator/document payloads and runtime summary now rely on backend commercial truth.
- 2026-03-28 to 2026-03-30: generator integration, runtime verification, CWU/backend explainability, and configurator/runtime alignment were materially hardened; those results remain reflected in canonical docs and decisions.
- 2026-03-21: PHP backend engines became the sole active runtime owners for OZC, selection, buffer, and pricing; JS behavior remains reference/parity only.

## In progress

- keep memory-bank concise in future work,
- maintain the new authority model during future contract/runtime/document updates.

## Next

1. Keep copied generator-facing schema artifacts reference-only unless they are explicitly verified against owning-repo/runtime truth.
2. Continue reducing duplicated prose across contract docs by linking back to canonical homes instead of restating whole sections.
3. Use the new read matrix and source-of-truth index as the default path for future Cursor agent sessions.
4. Verify raw file content before reacting to terminal-only rendering oddities.

## Update rule

- Keep this file condensed.
- Capture major milestones, not every local experiment.
- Use canonical docs for durable system truth.

## 2026-05-06 GitNexus OZC Comprehensive Audit

- Installed GitNexus MCP via `npx gitnexus setup` and indexed repo `kalk-top` (`8,113 nodes`, `14,142 edges`, `311 clusters`, `300 flows`).
- Replaced the OZC method audit with a comprehensive P0/P1/P2 report at `docs/architecture/ozc-professional-method-audit.md`.
- Key P0 risks: additive kW corrections double-count physics; gross/net area semantics shrink heated area; sloped roof is inferred as attic; annual heat-pump cost is inflated by HDD model, missing recovery in annual ventilation, static SCOP/tariff, and CWU/CO cost split bug.
- Recommendation: fix canonical design load first, then annual/cost semantics, then ISO 13370/ISO 52016-grade professional model.
