# memory-bank — kalk-top

Per-repo agent memory for **kalk-top only**. Cross-repo chronology and ecosystem proof live in Desktop `knowledge/` (see below).

## Files in this repo

| File                                                                         | Role                                    |
| ---------------------------------------------------------------------------- | --------------------------------------- |
| `current-state.md`                                                           | Stable operational summary for kalk-top |
| `progress.md`                                                                | Rare milestones                         |
| `navigation.md`                                                              | Task routing inside kalk-top            |
| `agent-handover.md` | Per-session handover (`## YYYY-MM-DD`) |
| `project-brief.md`, `decisions.md`, `open-questions.md`, `active-context.md` | Scoped context — **P7 PDF mapping in `open-questions.md`** |
| `docs/architecture/BACKLOG_RESOLUTIONS_2026-06-04.md` | Session closeout: problems 1–8, owner model, verification |

## Write after a session

1. Update `current-state.md` if stable truth changed.
2. For substantive work: add notes to `progress.md` or ecosystem docs — **or** if you use handover-style logs, create `agent-handover.md` with `## YYYY-MM-DD` (optional for kalk-top; gmail-agent uses this heavily).

## Multi-workspace / cross-repo

When Cursor includes `C:\Users\compg\Desktop\knowledge\`:

- Read and follow `knowledge/MEMORY_GOVERNANCE.md`.
- Append the day to `knowledge/timeline/YYYY-MM.md`.
- Contract/API changes → `knowledge/TOPINSTAL-KERNEL-GRAPH.yaml` + `group sync` per workspace `.cursorrules`.

Canonical contracts: `docs/contracts/`, `docs/ecosystem/TOPINSTAL_ECOSYSTEM_STATE.md`.
