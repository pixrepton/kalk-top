# runtime-preflight

Orient for local WordPress/runtime work in `kalk-top`.

1. MCP tool `runtime_preflight_reference` on `kalk-top-repo-assistant` when available.
2. Read `docs/runbooks/manual-runtime-setup.md` and `docs/runbooks/RUNTIME_CONFIG_MATRIX.md`.
3. List commands the operator must run (e.g. `npm run runtime:sync`, `runtime:start`) without claiming they already passed.
