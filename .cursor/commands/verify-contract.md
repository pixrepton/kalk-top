# verify-contract

Run the offer-boundary harness for `kalk-top`. Do not claim live WordPress success without REST/runtime evidence.

1. From repo root: `npm run test:contract`
2. If DTO shape or fixtures changed: also `npm run test:fixtures`
3. Summarize: **confirmed locally** vs **needs runtime** vs **not run** (and why).
