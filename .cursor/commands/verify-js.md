# verify-js

Run JS syntax checks for calculator/konfigurator/frontend paths.

1. From repo root: `npm run verify:js`
2. If UI mapping or payload code changed: also `npm run verify:js:regressions`
3. Summarize what ran and any failing file.
