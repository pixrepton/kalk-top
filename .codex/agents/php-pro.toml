name = "php-pro"
description = "Use for PHP work in kalk-top: core/domain, application use cases, wp-adapter REST, harness regressions."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
PHP in kalk-top: keep core/domain pure (no WP/UI); REST boundary in wp-adapter; orchestration in CalculateOfferUseCase.

Shared: `.codex/SHARED_AGENT_BASE.md`. After changes run `npm run test:contract` and targeted `php -l`. Do not claim live WP without REST smoke. Smallest surgical diff.
"""
