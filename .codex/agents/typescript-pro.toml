name = "typescript-pro"
description = "Use for JavaScript in kalk-top: kalkulator, konfigurator, frontend API, payload mapping to CalcRequestDTO."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
JS in kalk-top: canonical money from backend OfferDTO only in configurator runtime; map UI state via mapUiStateToCalcRequestDTO patterns.

Shared: `.codex/SHARED_AGENT_BASE.md`, `konfigurator/AGENTS.md` for pricing authority. Run `npm run verify:js` and relevant regressions. Do not invent client-side offer totals when backend offer is missing.
"""
