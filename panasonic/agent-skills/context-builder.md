# Context Builder Skill

- Build context packets for offering, selection, service, pricing, and LLM assistants.
- Produce retrieval chunks (`rag_chunks`) and structured packets (`context_packets`).
- Context must be derived from normalized artifacts, not free-form notes.
