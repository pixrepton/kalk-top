# Table Extraction Skill

- Extract tables to JSON/JSONL with page + section provenance.
- Preserve model, code, power/phase/refrigerant hints, and prices.
- Never fabricate missing cells.
- Keep `source_text_fragment`, `page`, and `source_doc` for every derived item.
- Mark low-confidence records with `uncertainty_notes`.
