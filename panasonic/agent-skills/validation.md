# Validation Skill

- Validate JSON/JSONL syntax and schema compliance.
- Check required provenance fields (`page`, `source_doc`, `section`).
- Detect duplicates, category drift, and missing model codes in key categories.
- Report uncertainty and quality gaps in dedicated reports.
