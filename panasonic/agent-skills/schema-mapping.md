# Schema Mapping Skill

- Map all extracted rows to canonical `panasonic_catalog_item` schema.
- Keep shared fields stable across categories.
- Store category-specific details in `category_specific`.
- Maintain category dictionary and generation/family vocabulary.
