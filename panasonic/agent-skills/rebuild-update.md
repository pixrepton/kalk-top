# Rebuild / Update Skill

- Re-run extraction+normalization from a new PDF.
- Compare new `pricing_index.json` against previous baseline.
- Emit added/removed/price-changed model report.
- Refresh memory/knowledge/context artifacts and produce update changelog.
